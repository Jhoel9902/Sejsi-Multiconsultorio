<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Error <%= statusCode %> - Sejsi Multiconsultorio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <style>
      :root {
        --primary-color: #1abc9c;
        --primary-dark: #0f9a6f;
      }
      
      body {
        background: linear-gradient(135deg, #f5f7fb 0%, #e8f4f8 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      
      .error-container {
        background: white;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        padding: 3rem;
        max-width: 600px;
        text-align: center;
      }
      
      .error-code {
        font-size: 5rem;
        font-weight: bold;
        color: var(--primary-color);
        margin-bottom: 1rem;
      }
      
      .error-message {
        font-size: 1.5rem;
        color: #333;
        margin-bottom: 1rem;
      }
      
      .error-description {
        color: #666;
        margin-bottom: 2rem;
        font-size: 1.1rem;
      }
      
      .error-icon {
        font-size: 4rem;
        color: var(--primary-dark);
        margin-bottom: 1rem;
      }
      
      .stack-trace {
        background: #f8f9fa;
        border: 1px solid #dee2e6;
        border-radius: 8px;
        padding: 1rem;
        margin: 2rem 0;
        text-align: left;
        max-height: 300px;
        overflow-y: auto;
        font-family: 'Courier New', monospace;
        font-size: 0.85rem;
        color: #d32f2f;
      }
      
      .stack-trace code {
        color: #d32f2f;
        word-break: break-all;
      }
      
      .action-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
      }
      
      .btn-primary {
        background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
        border: none;
      }
      
      .btn-primary:hover {
        background: linear-gradient(135deg, #18a888, #0d845f);
        color: white;
      }
    </style>
  </head>
  <body>
    <div class="error-container">
      <div class="error-icon">
        <i class="bi bi-exclamation-triangle"></i>
      </div>
      
      <div class="error-code"><%= statusCode %></div>
      <div class="error-message"><%= message %></div>
      
      <% if (statusCode === 404) { %>
        <div class="error-description">La página que buscas no existe.</div>
      <% } else if (statusCode === 403) { %>
        <div class="error-description">No tienes permiso para acceder a este recurso.</div>
      <% } else if (statusCode === 500) { %>
        <div class="error-description">Algo salió mal en el servidor. Nuestro equipo ha sido notificado.</div>
      <% } else { %>
        <div class="error-description">Ha ocurrido un error. Por favor, intenta de nuevo.</div>
      <% } %>
      
      <% if (details) { %>
        <div class="stack-trace">
          <strong>Detalles técnicos (Desarrollo):</strong>
          <code><pre><%= details %></pre></code>
        </div>
      <% } %>
      
      <div class="action-buttons">
        <a href="/dashboard" class="btn btn-primary btn-lg">
          <i class="bi bi-house me-2"></i>Ir al Dashboard
        </a>
        <a href="javascript:history.back()" class="btn btn-outline-secondary btn-lg">
          <i class="bi bi-arrow-left me-2"></i>Volver Atrás
        </a>
      </div>
      
      <hr class="my-3">
      <small class="text-muted">
        Sejsi Multiconsultorio v1.0.0 | 
        <a href="/health" class="text-decoration-none">Estado del Servidor</a>
      </small>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
